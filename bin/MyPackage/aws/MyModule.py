"""
 Functions : If we want reuse same code again and again in
 current program

 If we want reuse any other program then we can
 keep those definitions in this file.
 Since this file having only definitions, we called as
 Modules

 Packages : If we have more modules and
 if we want to keep in a folders & sub folders then we can keep.
 Since we have only modules, we are calling it as PACKAGES

 Finally,
 Module : Collection of definitions
 Package : Collection of Modules
 """

my_company = "CGI"


def add(a, b):
    return a + b